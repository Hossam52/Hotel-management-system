import 'package:flutter/material.dart';

class SupervisorOrderDetailsLayout extends StatelessWidget {
  const SupervisorOrderDetailsLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
